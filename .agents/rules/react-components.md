---
activation: apps/**/*.tsx
---

When working with React components in this project, adhere to the following rules:

1. **Functional Components**: Use functional components and React Hooks.
2. **Testing**: Always ensure that newly created or modified components are properly tested. Refer to the `react-testing` skill for the appropriate testing libraries (Vitest, React Testing Library) and conventions.
3. **Styling**: Ensure styles align with the project's design system.
4. **Code Quality**: Ensure the code satisfies ESLint rules and doesn't introduce TypeScript errors.
